import sys

class Quizzer:
    def __init__(self):
        self.alive = True
        sys.stdout.write('Enter the number of the chord set you\'d like to'
                         'practice.\n')
        # show chord sets and number them
        # gather input, check if correct, proceed

        
        self.quizzing = True
